# Tuples are ordered collection of n values of any type.
# Tuple supports indexing. It is immutable and hashable if all its members are hashable.

t1 = (1, 2, 3)
t2 = ('a', 1, 'Test Phrase', (1, 2))
print(t2[2], t2[3])